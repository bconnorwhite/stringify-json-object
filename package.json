{
  "name": "stringify-json-object",
  "version": "1.0.2",
  "description": "Stringify and format a JSON object.",
  "author": "Connor White",
  "license": "MIT",
  "keywords": [
    "json",
    "stringify",
    "format",
    "pretty",
    "typed",
    "object"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bconnorwhite/stringify-json-object.git"
  },
  "main": "./build/index.js",
  "scripts": {
    "build": "bob build",
    "prepublishOnly": "yarn build && yarn test",
    "test": "yarn jest"
  },
  "dependencies": {
    "@bconnorwhite/json-types": "^1.0.2"
  },
  "devDependencies": {
    "@bconnorwhite/bob": "^1.9.5",
    "jest": "^26.4.2"
  }
}
